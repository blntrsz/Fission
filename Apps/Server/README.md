# Server

The Fission server is organized by feature and depends on `FissionCore`.

- `Sources/App` — server startup and dependency composition
- `Sources/Threads` — Thread endpoints and server-specific adapters
